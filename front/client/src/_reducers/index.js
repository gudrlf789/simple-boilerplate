import { combineReducers } from 'redux';
//import user from './user_reducer';

// redux의 combineReducers를 이용해서 전체 reducer를 관리하는 rootReducer를 만든다.
const rootReducer = combineReducers({

})

export default rootReducer;